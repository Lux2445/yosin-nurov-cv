# Archive Structure

This directory stores historical/reference materials that are not part of active runtime code.

- `reference/`: static reference snapshots from the original Vaalentin 2015 resume.
- `upstream-src/`: original upstream source tree preserved for comparison and asset recovery.
